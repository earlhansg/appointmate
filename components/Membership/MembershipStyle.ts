import { StyleSheet } from "react-native";

export const MembershipStyle = StyleSheet.create({
  imageButton: {
    padding: 15,
    margin: 10,
    borderRadius: 10,
    borderColor: "#e0e0e0",
    borderWidth: 1.5
  },
});
