import { StyleSheet } from "react-native";

export const ProStyle = StyleSheet.create({
  primaryText: {
    fontSize: 17,
    fontWeight: "600",
    color: "#ffffff"
  },
  secondaryText: {
    fontSize: 12,
    marginTop: 5,
    color: "#ffffff"
  }
});
