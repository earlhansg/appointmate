import { StyleSheet } from "react-native";

export const TopShopsStyle = StyleSheet.create({
  container: {
    marginTop: 20,
    marginLeft: 10
  },
  header: {
    fontSize: 15,
    fontWeight: "500"
  }
});