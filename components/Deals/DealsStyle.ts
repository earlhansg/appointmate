import { StyleSheet } from "react-native";

export const DealsStyle = StyleSheet.create({
  container: {
    // marginTop: 20
    flexDirection: "row"
  },
  textContainer: {
    paddingRight: 3
  },
  text: {
    textAlign: "center",
    fontSize: 11,
    fontWeight: "600"
  }
});