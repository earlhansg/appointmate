import { StyleSheet } from "react-native";

export const TopFreelancersStyle = StyleSheet.create({
  container: {
    marginTop: 20,
    marginLeft: 10
  },
  header: {
    fontSize: 15,
    fontWeight: "500",
    marginBottom: 5,
  }
});